# Core dependencies (currently using only standard library)
# Add external packages here if needed

# For testing (optional)
pytest

# For code quality and pre-commit hooks
pre-commit
black
isort
flake8
pylint